\* CX-S4.2 — exhaustive check of the cluster admission safety invariant.
\* A 3-peer universe already exercises every join/leave/reconcile interleaving that can violate
\* admitted (subseteq) allowed (the offboard-eviction window). Run:
\*   java -jar tla2tools.jar -config ClusterAdmission.cfg ClusterAdmission.tla
SPECIFICATION Spec
CONSTANTS Peers = {p1, p2, p3}
INVARIANT AdmittedSubsetAllowed
