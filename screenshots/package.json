{
  "name": "citrate-core-screenshots",
  "private": true,
  "version": "0.1.0",
  "license": "BUSL-1.1",
  "description": "Reproducible docs screenshot harness for Citrate Core (runs the app in sim mode via Playwright).",
  "type": "module",
  "scripts": {
    "capture": "playwright test",
    "capture:headed": "playwright test --headed",
    "install-browser": "playwright install chromium",
    "postinstall": "playwright install chromium",
    "precapture": "playwright install chromium",
    "precapture:headed": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
