---
created: 2026-07-13
branch: docs/b1-5-scope
author: Claude Fable 5, directed by @SaulBuilds
status: scoped — dispatching build
sprint: CORE-B1 / work-package B1.5 (full custody adversarial suite → Phase B sign-off)
rule8: yes — the consolidated custody/signing evidence bundle for Phase B
depends_on:
  - B1.1 keystore, B1.2 ceremony, B1.3 connector, B1.4 broadcast — all MERGED
carries_forward:
  - B1.4 F-1 (LOW): signer-guard test covers only sign_message, not sign_transaction
  - B1.4 F-2 (LOW): approve_and_broadcast doesn't assert from == wallet
---

# B1.5 — full custody adversarial suite + Phase-B sign-off

## What B1.5 is
The custody/signing spine is built (B1.1–B1.4). B1.5 does NOT add product surface — it
**consolidates the complete adversarial evidence** for the whole spine into one @rule8
bundle, closes the two carried LOW should-fixes, fills any coverage gaps in the B1 ADV table,
and stands as the **Phase-B sign-off** (independent review = Phase B closed). Effort: thorough.

## WP-1 — audit the ADV-1…ADV-10 coverage (the sprint's custody table)
For EACH case, locate the existing test(s) across B1.1–B1.4, confirm it's non-vacuous
(negative-control where a guard exists), and ADD a test if the case is only partially covered.
Produce a coverage matrix (case → test(s) → file:line → data source → neg-control result).
| # | Property | Likely home |
|---|---|---|
| ADV-1 | sign without ceremony approval → impossible | ceremony (both signers) |
| ADV-2 | any invoke command returns key/seed/entropy → none | command registry + compile barrier |
| ADV-3 | sign/approve while vault locked → fail closed | ceremony + wallet |
| ADV-4 | key/seed/mnemonic plaintext at rest → ciphertext only; no keys.json | custody raw-grep |
| ADV-5 | malicious origin spoofs benign intent → true origin shown; undecodable raw-gated | ceremony + txdecode |
| ADV-6 | Approve default-focused / approve-latest → not pre-selected; explicit id | ceremony |
| ADV-7 | sidecar/agent signs directly (bypass ceremony) → impossible | signer gating |
| ADV-8 | key/seed in logs / errors / Debug → absent | whole path (wallet/ceremony/rpc/txdecode) |
| ADV-9 | key survives after signing → zeroized | wallet/ceremony |
| ADV-10 | replay/duplicate one approval → one approval = one signature/broadcast | ceremony |

## WP-2 — close the carried should-fixes
- **F-1 (from B1.4 review):** extend the structural signer-guard test to ALSO forbid
  `wallet::sign_transaction` outside `ceremony::approve*` (today it only covers
  `sign_message`). Negative-control it (inject a `sign_transaction` call outside ceremony →
  the guard test must fail). Both signers reachable ONLY from ceremony approval.
- **F-2 (from B1.4 review):** in `approve_and_broadcast`, assert `from == the vault wallet
  address` BEFORE signing/nonce-fetch; mismatch → a clear error (not a downstream node
  nonce-mismatch rejection). Test both branches.

## WP-3 — residual triage (decide + document each)
- B1.2 R2 (locked → `NoWallet` label vs `VaultLocked`) — fix the label OR document the accepted
  UX gap (fail-closed already holds).
- B1.3 INFO — per-request origin capture (vs construction-time) — note for the multi-dapp
  future (single-origin shell today); no code change unless cheap.
- B140-2 (INFO, upstream wallet-core) — zeroize the RLP signing payload — note as upstream
  defense-in-depth, out of citrate-core scope.

## WP-4 — the evidence bundle
A single doc `B1.5_EVIDENCE.md` (in the sprint dir) mapping every ADV case + F-1/F-2 to its
test + result — the artifact the Phase-B sign-off review (and later the S7.5 Tier-1 audit)
consumes. State the honest, non-headless gaps explicitly (live OS keyring; interactive
approval UI; the live-broadcast HITL gesture stood-in — with the real confirmed tx
`0xd40ce789…` @ block 380644 as the end-to-end proof).

## Acceptance (Rule 11)
- Every ADV-1…ADV-10 case has a named, non-vacuous test (coverage matrix, with neg-controls).
- F-1: `sign_transaction` is guarded like `sign_message` (neg-control proven).
- F-2: `from != wallet` → clear pre-sign error (test).
- Gates green (cargo/vitest counts up, clippy -D, audit 0 lean); no regression to B1.1–B1.4.

## Definition of done (closes Phase B)
- The consolidated adversarial suite is green and non-vacuous; both should-fixes closed;
  residuals triaged; the evidence bundle written.
- Build-and-stop → INDEPENDENT review = **Phase-B custody sign-off**. Owner merges cleared code.
- Update the sprint: B1 (and Phase B) marked complete; test-count baseline recorded.
- CLAUDE.md Rule 3 already reflects the ceremony — verify it still matches after F-1/F-2.

## Out of scope
- New product surface (Phase C node/storage, Phase D money). Sponsored UserOps. EIP-1559.
- The ChatGPT cross-model quorum leg (standing, owner) — noted, not blocking.

## Dispatch
@rule8 consolidation + the two LOW fixes; build-and-stop → independent Phase-B sign-off review.
