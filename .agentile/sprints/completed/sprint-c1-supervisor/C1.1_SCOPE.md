---
created: 2026-07-13
branch: docs/c1-1-scope
author: Claude Fable 5, directed by @SaulBuilds
status: scoped — dispatching
sprint: CORE-C1 / C1.1 (citrate-node under the supervisor) — @rule8 (encrypted data dir + keyring key)
grounded_in:
  - citrate-chain/node — bin name `citrate`, embeds testnet-beta config, joins via boot1/2/3.citrate.ai:30303
  - src-tauri/src/supervisor.rs (C1.0/C1.0b, on main) — the SidecarSupervisor
  - src/bridge/domains.ts NodeDomain { status(), start(), stop() } — the sim seam to wire
depends_on:
  - C1.0 + C1.0b (supervisor) — MERGED
---

# C1.1 — citrate-node under the supervisor @rule8

## Locked owner decisions (2026-07-13)
- **D-C1-1: bundle the node as a Tauri sidecar** (externalBin), not download-on-demand.
- **D-C1-2: bounded sync-progress proof** (automated) + a documented full 0→head manual run.

## Carried finding (address here)
- **C1.0b-1 (MEDIUM):** the supervisor's `healthy_after` reset doesn't cover a daemon that
  flaps *faster* than `healthy_after` → still reaches permanent `Failed`. C1.1 must pick a
  deliberate policy for the node (a real daemon that may legitimately restart on reorg/crash):
  either tune `healthy_after` for node behavior, OR implement a decaying / sliding-window
  failure policy (recommend the latter if cheap — a node restarting hourly should never be
  permanently killed). Decide + justify; add the model transition + test if the policy changes.

## Work packages
### C1.1-WP0 — Tauri sidecar bundling
- Configure Tauri `externalBin` for the `citrate` node binary (target-triple naming:
  `citrate-<triple>`). For dev/proof, build the node for the current platform and place it in
  `src-tauri/binaries/`; document the per-platform CI cross-build as an S7 release item (CI
  cannot build the full node — heavy/private-repo). Rule-12 `[[drift]]` entry pinning the
  citrate-node source rev in `citrate-federation/manifest.toml`.
- **Do NOT commit the binary** (large) — gitignore it; the build/CI places it. Acceptance:
  the Tauri config resolves the sidecar; a placeholder/dev binary spawns under the supervisor.

### C1.1-WP1 — encrypted data dir @rule8
- Ground how `citrate` takes its storage encryption key (the reroll handoff notes a "32-byte
  storage master key"; loss = unrecoverable chain). citrate-core mints/holds that key in the
  **OS keyring** (reuse the A2 custody pattern — key never on disk in clear) and passes it to
  the node (env/arg/stdin — whatever the node accepts; ground it, don't assume).
- **Acceptance (Rule 11):** raw-disk grep of the node data dir proves **ciphertext** (the
  ENCRYPT pattern); the storage key is in the keyring, not on disk. Back-up/loss caveat
  documented (the key is fund-relevant like the wallet seed).

### C1.1-WP2 — spawn under supervisor + NodeDomain wiring
- `NodeDomain.start` → supervisor spawns `citrate --network testnet` with the data dir +
  storage key; `status` → real sync state (height, peers, syncPct) from the node's local
  RPC/status API (ground the port/endpoint — core/api); `stop` → graceful (supervisor
  released). Apply the C1.0b-1 policy decision (healthy_after / decaying-window) tuned for the node.
- **Acceptance:** NodeDomain.status reflects the live node (height advances); start/stop drive
  the real process; a killed node → supervisor restarts it (crash record) unless intentionally stopped.

### C1.1-WP3 — bounded sync-progress proof (D-C1-2)
- **Automated (CI-safe):** supervisor+NodeDomain wiring tests use a STUB node helper (a
  test binary that emits status + writes to a data dir) — NO real network in CI.
- **Live proof (documented local run, like B1.4):** spawn the REAL node → it connects to
  boot1/2/3.citrate.ai:30303 → sync **height advances** over a bounded window (e.g. ≥ N blocks
  in M minutes) → capture the height delta + peer count as evidence; raw-disk ciphertext grep
  on the real data dir. Do NOT fake a sync — run it or hand me the exact command + capture.

## Test plan (agentile:test-plan)
- Unit/integration (CI): NodeDomain↔supervisor wiring against the stub node; status parsing;
  encrypted-data-dir key handling (key in fake keyring, ciphertext grep on the stub's output);
  the C1.0b-1 policy test if the policy changed. Tripwire: a ciphertext-at-rest grep test.
- Live-path (documented): the real bounded-sync proof. Formal: none new (supervisor already
  modeled; the node is external).
- Baseline ratchet: rust 156 → up; update `.agentile/coverage/BASELINE.md` at close.

## Definition of done (C1.1)
- The node runs under the supervisor with an encrypted (keyring-keyed) data dir; NodeDomain
  status/start/stop are live against the real node; the C1.0b-1 policy is decided + covered;
  bounded-sync proof captured (+ full-sync documented); sidecar bundling configured.
- Gates green (cargo/vitest up, clippy -D, audit 0); build-and-stop → independent review
  (@rule8). Owner merges cleared code. Honest gaps stated (full sync time; per-platform CI cross-build = S7).

## Out of scope
- C2 earnings (node-agent sweep + ContributionAccounting). C1.2 node-agent + signature bridge.
- Per-platform release cross-build of the node (S7). Full 0→head soak (S3.5).
