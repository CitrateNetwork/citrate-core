# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust workspace target (workspace root is repo root)
target/

# W4 (OAuth): dev-only OAuth client IDs/secrets for Google/Notion/GitHub MCP
# connections. Real credentials — NEVER committed. Ship path seals these in the
# OS keyring via Settings → Connections; this file is a dev-testing convenience
# read by src-tauri at startup. See docs/OAUTH_SETUP_RUNBOOK.md.
src-tauri/oauth.dev.json

# CORE-C1.1 (D-C1-1): the bundled citrate-node sidecar binary is large and is
# NOT committed. Tauri's externalBin resolves `binaries/citrate-<target-triple>`
# at bundle time; per-platform CI cross-build of these is an S7 item. Keep the
# directory's README, ignore the binaries.
src-tauri/binaries/*
!src-tauri/binaries/README.md

# Local AI model + llama.cpp build artifacts. gemma gguf (~4.7G) + bge embed
# model live under src-tauri/models/; the compiled llama.cpp runtime under
# src-tauri/llama/ (~22M). Both are fetched/built locally, never committed.
src-tauri/models/
src-tauri/llama/

# ─────────────────────────────────────────────────────────────────────
# Open-source hygiene — secret material & internal records (added 2026-09)
# History was scrubbed of these; this block prevents re-introduction.
# ─────────────────────────────────────────────────────────────────────

# Secret material — never in a public repo
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.p8
*.der
id_rsa
id_ed25519
*_private*.pem
*private_key*
*jwks*.json
**/secrets.json
**/secrets.yaml
**/secrets.yml
**/secrets.toml
mnemonic*
*seed-phrase*
*seed_phrase*

# Internal process / institutional-knowledge records — not for public repos
.agentile/
handoffs/
**/*HANDOFF*
**/*_HANDOFF*
**/*INCIDENT*
**/*EXEC_STATUS*
**/*COUNSEL*
**/*DEPLOY_LOG*
