// CX surface — Groups (C-19). Owned by lane s3 (CX-S3) after S0. Shell only.
import { SurfaceProps } from "./shared";
import { CxScaffold } from "./CxScaffold";

export function Groups(_props: SurfaceProps) {
  return (
    <CxScaffold
      title="Groups"
      sprint="CX-S3"
      detail="Message people 1:1 or in groups, end-to-end encrypted. A Group you own is administered with roles — invite, assign, and remove members — the same format as Citrate Comms."
    />
  );
}
